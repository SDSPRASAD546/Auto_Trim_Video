services:
  - type: web
    name: AutoTrimFF
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y ffmpeg
      pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 4 --bind 0.0.0.0:8000
